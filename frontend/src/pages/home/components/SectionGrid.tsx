import React from 'react'


type SectionGridProps = {
	title: string;
	songs: Song[];
	isLoading: boolean;
};

const SectionGrid = () => {
  return (
    <div>SectionGrid</div>
  )
}

export default SectionGrid